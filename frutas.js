const frutas = ['platano', 'manzana', 'platano', 'pera'];
const dinero = 1000;

//exportación de módulo. Se exportan los 2 dentro de un objeto.

module.exports = {
    frutas : frutas,
    dinero : dinero
}